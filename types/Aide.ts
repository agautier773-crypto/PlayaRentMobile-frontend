export type LocationEtape = {
  numero: number;
  titre: string;
  description: string;
};

export type PratiqueEtape = {
  numero: number;
  titre: string;
  description: string;
  imageKey?: string;
};